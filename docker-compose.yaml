version: '3.8'

services:
  app:
    build: .
    restart: unless-stopped
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - CHECK_INTERVAL_MINUTES=${CHECK_INTERVAL_MINUTES:-2}
      - HEADLESS=true
      - LOG_LEVEL=${LOG_LEVEL:-info}
